# Журнал решений и прогресса

Новые записи — сверху. Каждая запись: дата, этап, что сделано, решения и почему, что дальше.
Записи не удаляются и не переписываются задним числом — только дополняются.

## Текущий этап: 0 — Каркас проекта

---

## 2026-09-24 — Этап 0

**Сделано:**
- Создана структура каталогов по `CLAUDE.md`: `config/`, `data/{raw,processed}`,
  `src/lab/{data,engine,strategies,benchmarks,metrics,optimize,report}`, `tests/`, `reports/{data_quality,dev,wf,holdout}`.
- Документы протокола скопированы в `docs/`, `protocol.yaml` — в `config/`.
- `pyproject.toml` (src-layout, зависимости: pandas/numpy/pyarrow/pyyaml/matplotlib/mlflow/requests;
  опционально numba/joblib для скорости, pytest для тестов).
- `.gitignore`: `data/`, `mlruns/`, `*.parquet`, `.env`, `.venv/`.
- `src/lab/config.py`: типизированная загрузка и валидация `config/protocol.yaml` (dataclasses,
  `lru_cache`, проверки на здравый смысл — например, leverage должен быть 1x).
- `tests/test_config.py`: 3 теста (чтение реального конфига, отсутствующий файл, невалидные значения).
- Заготовки пакетов (`__init__.py` с докстрингом-описанием) во всех подпакетах `src/lab/*`,
  чтобы структура была видна сразу, даже пока код внутри не написан.

**Решения (и почему):**
- git-репозиторий пока не инициализирован — сначала пользователь смотрит структуру и подтверждает,
  что она понятна и ничего не нужно поменять (Этап 0, "Готово, если").
- `pyproject.toml` вместо `requirements.txt`: src-layout, `pip install -e .` даёт нормальный
  импорт `from lab...` без танцев с `PYTHONPATH` — проще объяснить один раз, чем на каждом этапе.
- MLflow не поднимался и `pytest` не запускался — по правилам работы агент не выполняет команды
  сам, ниже даны готовые команды для пользователя.

**Открытые вопросы к пользователю:**
- Устраивает ли структура каталогов и разбивка на пакеты?
- Подтвердить: инициализировать git-репозиторий и сделать первый коммит сейчас?
- Смотрим предложенные улучшения протокола (см. ответ в чате) — что фиксируем, что откладываем?

**Дальше:**
- После подтверждения — `git init`, первый коммит, затем Этап 1 (пул монет).

---

## 2026-09-24 — Этап 0 (проверка пользователем)

**Сделано:**
- Пользователь прогнал `pip install -e ".[dev,speed]"` и `pytest -v` — все 3 теста зелёные.
- `mlflow ui --backend-store-uri ./mlruns` упал: в mlflow 3.16 файловый backend-store переведён
  в режим "только для чтения", нужен backend на БД (SQLite/Postgres/...).

**Решения (и почему):**
- Локальный backend MLflow — `sqlite:///mlflow.db` в корне проекта, артефакты (trades.csv,
  графики) — по-прежнему в `./mlruns` через `--default-artifact-root`. Так надёжнее и при
  параллельных прогонах сетки на Этапе 7 (SQLite лучше файлового стора переживает конкурентную
  запись из нескольких процессов joblib).
- `mlflow.db` — это локальная инфраструктура запуска, а не константа протокола честности
  бэктеста, поэтому в `config/protocol.yaml` не идёт (файл только про то, что влияет на честность
  результата — комиссии, даты, размер позиции). URI backend-store фиксируется соглашением здесь,
  в JOURNAL, и в командах для пользователя.
- `mlflow.db` добавлен в `.gitignore` (как и `mlruns/`) — это локальный кэш реестра, а не
  часть репозитория; при необходимости реестр всегда можно пересобрать из прогонов заново.

**Дальше:**
- Пользователь запускает обновлённую команду ниже, проверяет, что MLflow UI открывается
  и создаёт тестовый run.
- После этого — `git init`, первый коммит, Этап 1.

---

## Решения, принятые до старта (из обсуждения с пользователем)

- Пул монет: топ-10 по капитализации на конец 2025 года (без BTC, ETH, стейблкоинов,
  обёрнутых токенов и монет без USDT-M перпетуала на Binance). Сегодняшний топ не используется —
  это знание будущего.
- 13 стратегий + вариант S01b + надстройки O1, O2 (`03_STRATEGIES.md`).
- Оптимизация параметров, ТФ и монет — да, но только на dev 2023–2025 через walk-forward.
  Holdout 2026 — один прогон заранее зафиксированных вариантов (`04_OPTIMIZATION.md`).
- Условия песочницы: Binance USDT-M, рыночные ордера, taker + 0.02% проскальзывания,
  10% equity на слот, плечо 1x, старт 10 000.
- Реестр испытаний — MLflow.

---

## 2026-09-24 — Этап 0, правки протокола (5 пунктов, по запросу пользователя)

**Сделано:**
- `account.max_slots_portfolio`: 10 → 5 (`config/protocol.yaml`, `src/lab/config.py`,
  `tests/test_config.py`, `01_PROTOCOL.md` §5.5, §6).
- Правило конкуренции за слот, когда сигналов на вход больше свободных слотов: открытые
  позиции сохраняют слот, новые сигналы заполняют свободные слоты по алфавиту символа Binance,
  остальные в этом баре не исполняются (`slot_contention_rule: existing_first_then_alpha`).
  Добавлен тест "Slot contention" в `02_ENGINE_SPEC.md` §7.
- Buy & Hold в портфельном режиме определён как отдельный пассивный бенчмарк на весь пул
  (10 монет), вес монеты = `slot_fraction × max_slots_portfolio / n_pool` = 5%, без
  ребалансировки (куплено один раз в начале периода, веса дрейфуют), вне очереди на слот
  (`01_PROTOCOL.md` §7).
- Физическая изоляция holdout-данных: после Этапа 2 файлы с `open_time >= holdout_start`
  переносятся в `data/processed/_holdout_locked/` — второй барьер поверх кода загрузчика
  (`05_PLAN.md` Этап 2, `CLAUDE.md`).
- Date-leak grep-тест: ни в одном файле стратегии (включая `community/`) не должно быть
  захардкоженных литералов `2026-*` (`02_ENGINE_SPEC.md` §7).
- DSR по умолчанию 50 испытаний для стратегий друзей — в отчёте и на строке лидерборда
  теперь явно помечается как предположение, а не факт (`06_LEADERBOARD.md` §5.3).
- `prepare()` кэшируется на диске (`.cache/prepare/<hash>.parquet`), не только в памяти
  процесса — переживает перезапуски между прогонами (`02_ENGINE_SPEC.md` §8, `.gitignore`).

**Решения (и почему):**
- Число слотов уменьшено с 10 до 5 по прямому указанию пользователя: у пула 10 монет,
  портфельный режим больше не гарантирует каждой монете свой слот — впервые возникает
  реальная конкуренция за слот, которой протокол раньше не касался.
- Алфавитный тай-брейк для конкуренции за слот выбран как самый нейтральный вариант:
  не использует силу сигнала или ожидаемую доходность, детерминирован и легко
  проверяется/повторяется вручную по одному прогону. Обсуждалось с пользователем
  (см. чат) — альтернативы (случайный выбор с фиксированным сидом, ранжирование по силе
  сигнала) были предложены, но не выбраны явно; решение принято по умолчанию, можно
  пересмотреть после первых прогонов Этапа 3, если результат будет казаться нечестным.
- Buy & Hold сделан отдельным от лимита слотов сознательно: иначе при 10 монетах и 5 слотах
  половина пула была бы произвольно и навсегда исключена из бенчмарка с самого начала периода
  (B&H никогда не выходит из позиций, значит конкуренция за слот решалась бы только один раз
  в начале и была бы не показательна). Пользователь не имел предпочтения — выбран
  рекомендованный вариант (весь пул, без ребалансировки).
- `.cache/` — не в реестре MLflow и не в `data/`, отдельная директория, потому что это кэш
  промежуточных вычислений (детерминированная функция от кода+параметров), а не результат
  прогона и не сырые данные — можно удалить в любой момент без потери информации.

**Открытые вопросы к пользователю:**
- Алфавитный тай-брейк для слотов — устраивает, или предпочитаете другой вариант
  (случайный с фиксированным сидом / ранжирование по силе сигнала)?
- Определение Buy & Hold (весь пул, без ребалансировки, вне очереди на слот) — подтвердить.

**Дальше:**
- При согласии — коммит этих правок, переход к Этапу 1 (пул монет).

---

## 2026-09-24 — Отказ от MLflow в пользу простого реестра

**Сделано:**
- Убран MLflow как хранилище реестра испытаний. Вместо него — append-only
  `reports/registry.csv` + `src/lab/registry.py` (`log_run()`, `count_trials()`), без сервера
  и без БД. Обновлены: `CLAUDE.md`, `01_PROTOCOL.md` §9, `02_ENGINE_SPEC.md` §6–7, `05_PLAN.md`
  Этап 0/3, `config/protocol.yaml` (`registry.backend: csv`), `pyproject.toml` (убрана
  зависимость `mlflow`), удалена пустая `mlruns/`, из `.gitignore` убраны `mlruns/`, `mlflow.db`.

**Решения (и почему):**
- Пользователь: «mlflow нам как будто не нужен, достаточно просто получить лидерборд всех
  стратегий». Согласен: то, что реально требует протокол (01_PROTOCOL §9) — это не MLflow
  как система, а неизменяемый журнал числа испытаний для DSR и артефакты каждого прогона.
  Append-only CSV даёт обе гарантии без сервера, БД и её версионных проблем (см. запись про
  `sqlite:///mlflow.db` — MLflow 3.16 уже один раз потребовал внимания на ровном месте).
  Заодно `reports/registry.csv`, в отличие от MLflow UI, читается глазами и попадает в git —
  сама история коммитов становится дополнительным свидетельством, что строки не менялись
  задним числом.
- `reports/registry.csv` **коммитится в git** (не в `.gitignore`), в отличие от `data/` —
  это часть журнала честности протокола, а не сырые данные, его нельзя «просто пересобрать».
  Артефакты отдельных прогонов внутри `reports/<stage>/<run_id>/` (`equity.parquet` и т. п.)
  по-прежнему подпадают под общий `*.parquet` в `.gitignore`.
- `mlflow` можно вручную удалить из виртуального окружения (необязательно, просто чтобы не
  захламлять `pip list`): `pip uninstall mlflow mlflow-skinny mlflow-tracing -y`.

**Дальше:**
- `src/lab/registry.py` появится на Этапе 3 вместе с движком (первый реальный `log_run()`).
- Переходим к Этапу 1: пул из 10 монет.
